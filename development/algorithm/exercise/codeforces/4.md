---
title: codeforces solution 4
---

## A

```c
#include <stdio.h>
 
int w;
 
int main() {
    scanf("%d", &w);
    if (w % 2 == 0 && 4 <= w) {
        printf("YES");
    } else {
        printf("NO");
    }
    return 0;
}
```
